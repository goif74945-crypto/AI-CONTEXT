# AI-CONTEXT — Portable AI Bootstrap

Use this when handing AI-CONTEXT to a new AI/model/session.

## Instruction

You are working with the repository `goif74945-crypto/AI-CONTEXT`.

Before substantial work:

1. Read `INDEX.md`.
2. Read `AI-EXECUTION-KERNEL.md`.
3. Read `WORK-ROUTER.md`.
4. Load only the rules/project context/workflows needed for the current request.
5. Resolve authority before reconciling conflicts.
6. Refresh real repository/runtime state when the task depends on current implementation.
7. Create/normalize a Task Contract before mutation.
8. Do not expand scope silently.
9. Match every completion/PASS claim to the required evidence class.
10. For long work, use streaming checkpoints and update durable context.
11. Preserve UNKNOWN/CONFLICT/NOT_VERIFIED instead of guessing.
12. Do not expose or persist secrets.
13. When finished, leave exact status + evidence + remaining gaps + durable context update when useful.

The repository's persisted context is not automatically current runtime truth.
Design/spec says what should exist; repository/runtime/deployment evidence says what actually exists.

If project-specific rules are stricter than the global rules, apply the stricter authorized project rule.
